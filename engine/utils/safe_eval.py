"""


--- L9_META ---l9_schema: 1origin: engine-specificengine: graphlayer: [config]tags: [utils, safe-eval]owner: engine-teamstatus: active--- /L9_META ---engine/utils/safe_eval.pyAST-whitelist expression evaluator. Replaces eval() for derived parametersand unit conversion formulas."""from __future__ import annotationsimport astimport mathimport operatorfrom collections.abc import Callablefrom typing import Any# Maximum exponent value to prevent DoS via 9**9**9**9_MAX_EXPONENT = 1000def _check_pow_overflow(base: float, exp: float) -> None:    """Check if power operation would overflow. Raises ValueError if unsafe."""    if base == 0 or exp <= 0:        return    # Calculate log10 of result to check for overflow    log_result: float | None = None    calc_error: Exception | None = None    try:        log_result = exp * math.log10(abs(base))    except (ValueError, OverflowError) as e:        calc_error = e    if calc_error is not None:        raise ValueError(f"Power operation {base}**{exp} is unsafe") from calc_error    if log_result is not None and log_result > 308:  # Approaching float overflow        raise ValueError(f"Power operation {base}**{exp} would overflow")def _safe_pow(base: Any, exp: Any) -> Any:    """Power operation with DoS protection."""    if isinstance(exp, (int, float)) and abs(exp) > _MAX_EXPONENT:        raise ValueError(f"Exponent {exp} exceeds maximum allowed ({_MAX_EXPONENT})")    if isinstance(base, (int, float)) and isinstance(exp, (int, float)):        _check_pow_overflow(float(base), float(exp))    return operator.pow(base, exp)_SAFE_BINARY_OPS: dict[type[ast.operator], Callable[[Any, Any], Any]] = {    ast.Add: operator.add,    ast.Sub: operator.sub,    ast.Mult: operator.mul,    ast.Div: operator.truediv,    ast.FloorDiv: operator.floordiv,    ast.Mod: operator.mod,    ast.Pow: _safe_pow,  # Use protected power function}_SAFE_UNARY_OPS: dict[type[ast.unaryop], Callable[[Any], Any]] = {    ast.USub: operator.neg,    ast.UAdd: operator.pos,}_SAFE_FUNCS = {    "abs": abs,    "min": min,    "max": max,    "round": round,    "log": math.log,    "exp": math.exp,    "sqrt": math.sqrt,}def safe_eval(expression: str, context: dict[str, Any]) -> Any:    """    Evaluate a simple algebraic expression with variable substitution.    Only allows arithmetic operations, numeric literals, and whitelisted functions.    Raises ValueError on disallowed AST node types.    """    tree = ast.parse(expression, mode="eval")    return _eval_node(tree.body, context)def _eval_node(node: ast.AST, ctx: dict[str, Any]) -> Any:    if isinstance(node, ast.Constant):        if not isinstance(node.value, (int, float)):            raise ValueError(f"Only numeric constants allowed, got {type(node.value).__name__}")        return node.value    if isinstance(node, ast.Name):        if node.id in _SAFE_FUNCS:            return _SAFE_FUNCS[node.id]        if node.id not in ctx:            raise ValueError(f"Unknown variable: {node.id!r}")        return ctx[node.id]    if isinstance(node, ast.BinOp):        bin_op_type = type(node.op)        if bin_op_type not in _SAFE_BINARY_OPS:            raise ValueError(f"Unsupported operator: {bin_op_type.__name__}")        bin_op_func = _SAFE_BINARY_OPS[bin_op_type]        return bin_op_func(_eval_node(node.left, ctx), _eval_node(node.right, ctx))    if isinstance(node, ast.UnaryOp):        unary_op_type = type(node.op)        if unary_op_type not in _SAFE_UNARY_OPS:            raise ValueError(f"Unsupported unary operator: {unary_op_type.__name__}")        unary_op_func = _SAFE_UNARY_OPS[unary_op_type]        return unary_op_func(_eval_node(node.operand, ctx))    if isinstance(node, ast.Call):        func = _eval_node(node.func, ctx)        if func not in _SAFE_FUNCS.values():            raise ValueError(f"Function not whitelisted: {ast.dump(node.func)}")        args = [_eval_node(a, ctx) for a in node.args]        return func(*args)    raise ValueError(f"Disallowed expression node: {type(node).__name__}")
